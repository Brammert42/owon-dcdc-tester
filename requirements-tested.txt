# Direct dependency versions used for the robustness validation (Python 3.11.15).
# Transitive packages remain resolved by pip; this is not a full environment lock.
PySide6==6.11.1
matplotlib==3.11.0
pyserial==3.5
flask==3.1.3
flask-socketio==5.6.1
